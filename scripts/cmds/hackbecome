module.exports = {
  config: {
    name: "hackbecome",
    version: "1.0",
    author: "L'Uchiha Perdu",
    role: 0,
    shortDescription: "Devenir hackeur",
    longDescription: "Répondez correctement aux défis pour devenir hackeur et pouvoir utiliser /hack2.",
    category: "économie",
    guide: "{p}hackbecome"
  },

  onStart: async function ({ message, event }) {
    message.reply("🤖 **Test pour devenir hackeur :**\n\n" +
      "1️⃣ **Factorisation** : Trouvez les facteurs premiers de **91**.\n" +
      "2️⃣ **Question informatique** : Quel protocole est utilisé pour sécuriser les sites web (HTTP ou HTTPS) ?\n\n" +
      "💬 Répondez avec : `/hackbecome Réponse1 Réponse2`"
    );
  },

  onReply: async function ({ args, message, event }) {
    if (args.length < 2) return message.reply("❌ Répondez avec `/hackbecome Réponse1 Réponse2`");

    const answer1 = args[0];
    const answer2 = args[1].toLowerCase();

    if (answer1 !== "7,13" || answer2 !== "https") {
      return message.reply("❌ Mauvaise réponse ! Essayez encore.");
    }

    let hackers = {};
    if (fs.existsSync("hackers.json")) {
      hackers = JSON.parse(fs.readFileSync("hackers.json"));
    }

    hackers[event.senderID] = true;
    fs.writeFileSync("hackers.json", JSON.stringify(hackers, null, 2));

    return message.reply("✅ **Félicitations !** Vous êtes maintenant un hackeur. Utilisez `/hack2 [UID]` pour pirater.");
  }
};